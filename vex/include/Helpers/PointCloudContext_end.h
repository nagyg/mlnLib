
	// user finish is above this

	ForceSubmitComponentArray(forceNames, forceVectors, forcePriorities, ptnum);

	if (writeDebugAttribs) {
		ForceCreateDebugAttribs(forceNames, forceVectors, ptnum);
	}

// end of the CVEX call
}   
 